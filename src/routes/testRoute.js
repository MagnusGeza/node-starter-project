const express = require("express");
// const { addToCart, removeFromCart } = require("../controllers/cartController");
// const { requireSignin, userMiddleware } = require("../middleware/middleware");

const router = express.Router();

// router.post("/user/cart/add", requireSignin, userMiddleware, addToCart);
// router.post("/user/cart/remove", requireSignin, userMiddleware, removeFromCart);

module.exports = router;
